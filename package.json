{
  "name": "@llm-grimoire/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "fetch-registry": "rm -rf registry && git clone --depth 1 https://github.com/llm-grimoire/registry.git registry",
    "dev": "npm run fetch-registry && astro dev",
    "build": "npm run fetch-registry && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.13",
    "astro": "^5.17.2",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.3"
  }
}
